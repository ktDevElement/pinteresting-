<%= image_tag @pin.image.url, size: "300" %>

<p>
  <strong>Description:</strong>
  <%= @pin.description %>
</p>

<% if @pin.user == current_user && @pin.user != nil %>
	<%= link_to 'Edit', edit_pin_path(@pin) %> |
<% end %>

<%= link_to 'Back', pins_path %>
